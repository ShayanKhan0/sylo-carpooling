# Alembic migrations will be configured here
# Run: alembic init migrations
# Then configure alembic.ini and env.py

"""
To initialize Alembic migrations:

1. Install alembic: pip install alembic
2. Run: alembic init app/db/migrations
3. Update alembic.ini with your database URL
4. Create first migration: alembic revision --autogenerate -m "Initial migration"
5. Apply migration: alembic upgrade head

For more info: https://alembic.sqlalchemy.org/en/latest/tutorial.html
"""
