# Contribution Guidelines

- Follow modular architecture; each functionality in its own directory/module.  
- Comment **every major and minor logic block** (especially backend routes, services, and database models).  
- Always create feature branches before merging to `main`.  
- Use clear commit messages: `feat(auth): added JWT verification middleware`.  
- Run linting and tests before every pull request.  
